{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core APIs including events, which the frontend uses to receive serial data.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
