{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core APIs including events, which the frontend uses to receive serial data, and the save dialog for choosing a session log file.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save"]
}
